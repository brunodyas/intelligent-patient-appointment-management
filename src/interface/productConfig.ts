export interface ConfigValue {
    product_type?: number | null,
    vendor?: number | null,
    product?: number | null,
    style_id?: number | null
}

export interface ListOption { name: string, id: number | null }